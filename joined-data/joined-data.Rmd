---
title: "joined-data"
output: html_document
---

## Set up
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(jsonlite)
library(httr)
library(rvest)
library(readxl)
library(tidyverse)
library(mdsr)   
library(tidyr)
library(ggplot2)
library(readr)
library(knitr)
```

## Join Census Data
```{r, message=FALSE}

data_pop <- pop_all_years
data_census <- final_census_df
data_gdp <- gdp_df

data_joined_pop_census <- left_join(data_pop, data_census, by = c("Year", "GEO.id", "GEO.id2", "GEO.display-label"))

data_joined_pop_census_gdp <- left_join(data_joined_pop_census, data_gdp, by = c("GEO.id2", "Year"))

# joined_pop_census <- full_join(population_data, census_data)


# full join doesn't work; creates duplicate rows. maybe need inner_join
# df1 <- gdp_df
# df2 <- inner_join(joined_census_data, df1)
# View(df2)
```
```{r}

```
