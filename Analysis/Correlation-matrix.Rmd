---
title: "Correlation-matrix"
output: html_document
---

## Set up
```{r setup, include=FALSE, message=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(jsonlite)
library(httr)
library(rvest)
library(readxl)
library(tidyverse)
library(mdsr)   
library(tidyr)
library(ggplot2)
library(readr)
library(knitr)
library(corrplot)
```

## Join Census Data
```{r, message=FALSE}
correlation_df <- FINAL_DF %>% 
  select(-X1, -msa_id, -msa, -year, -centlat, -centlon, -intptlat, -intptlon, -ua_census_id, -ua_census_name, -ua_pop_2010, -ua_fta_id, -ua_fta_name, -transit_modes) 


correlation_df <-  data.frame(lapply(correlation_df, function(x){ 
  as.numeric(x)
}))

cor_matrix <- cor(correlation_df, use = "complete.obs")

corrplot(cor_matrix, type = "upper", order = "hclust", 
         tl.col = "black", tl.cex = .6)


```

```{r}


```
